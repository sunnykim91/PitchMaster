-- ============================================================
-- 00064_pitchscore_drop_cross.sql
-- PitchScore — CROSS 능력치 비활성화
-- ============================================================
-- 사용자 결정 (44차 후속):
--   "코너킥, 프리킥을 차는 사람은 정해져있거든. 이 항목을 빼도 될거같아"
--   → 00063 에선 FREE_KICK 만 비활성화했으나 CROSS(코너킥/측면 크로스) 도
--      정해진 사람이 차는 영역이라 같은 이유로 비활성화.
--
-- 방식: applicable_sports = '{}' (legacy 평가 row 보존, UI/평가 자동 제외)
-- ============================================================

UPDATE player_attribute_codes
SET applicable_sports = ARRAY[]::text[]
WHERE code = 'CROSS';
